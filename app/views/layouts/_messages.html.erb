<% flash.each do |key, value| %>
  <div class="row">
    <div class="large-12 columns">
      <div data-alert class="alert-box <%= flash_class(key) %>">
        <%= value %>
      </div>
    </div>
  </div>
<% end %>
